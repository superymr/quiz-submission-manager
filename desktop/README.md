# Quiz 提交管家桌面版

## 修改并重新打包

1. 修改 `../outputs/quiz-local/` 中的源代码。
2. 在本目录执行 `npm install`（首次需要）。
3. 执行 `npm run dist`。
4. 新程序生成到 `../outputs/quiz-desktop/`。

桌面版运行数据保存在程序同目录的 `Quiz数据/` 文件夹。升级时关闭旧程序，只替换 `.exe`，不要删除该数据文件夹。
